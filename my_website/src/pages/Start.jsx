const Start = () => {
    return ( 
        <div className="text-white text-5xl" onClick={""}>      {/* implement function to handle clicking on the first page to route the user to the next page */}
            Click anywhere to get started                       {/* animate the text somehow? flashing bar at the end of the line like a terminal screen?  */}
        </div>
     );
}
 
export default Start;